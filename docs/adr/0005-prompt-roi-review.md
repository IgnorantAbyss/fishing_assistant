# ADR 0005: Human-Approved Prompt ROI

Status: accepted

The legacy ROI is positioned correctly but too broad. Multiple normalized candidates are visualized across sessions/states. No accuracy result, bright mask, or automatic heuristic may approve a candidate. Dataset materialization is blocked until a user explicitly writes `roi_status: approved` and coordinates.
